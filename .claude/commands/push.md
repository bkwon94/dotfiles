Push latest git changes to remote.

This can be done with simply:

```bash
git push
```

If there is no upstream branch, you can add one with:

```bash
git push --set-upstream origin <branch-name>
```
